Newb Data Generator 🛠️A simple, offline-first web tool designed to help developers and contributors easily generate JSON data snippets for the Newb Shaders Hub app.Stop wrestling with JSON syntax errors! This tool provides a user-friendly interface to create shader and developer profiles that match the app's strict data schema.Shutterstock Explore ✨ FeaturesDual Mode: Generate JSON for both Shaders (shader-list-testing.json) and Developers (developers/X.json).Dynamic Inputs: Add as many download links and screenshots as you need with a single click.Dark Mode Support: A beautiful, high-contrast UI that adapts to your system theme (or toggle it manually).Instant Preview: See the generated JSON update in real-time as you type.One-Click Copy: Copy the perfectly formatted JSON snippet to your clipboard instantly.Zero Dependencies: Runs entirely in the browser. No server, no installation required.🚀 How to Use1. SetupSince this is a single-file tool, you don't need npm or a server.Download the index.html file from this repository.Double-click it to open it in your web browser (Chrome, Edge, Firefox, etc.).2. Generating a Shader EntryUse this mode when you want to add a new shader to the main list.Select "Add Shader" at the top.Fill in the details:ID: A unique number for the shader.Creator ID: Must match the filename of a developer (e.g., 0_devendrn).Download Links: Add multiple mirrors (MCPACK, ZIP, etc.).Screenshots: Add URLs for the image gallery.Platforms: Check all supported devices.Copy the generated JSON from the right panel.Paste it into the array inside shader-list-testing.json.3. Generating a Developer ProfileUse this mode when adding a new creator to the ecosystem.Select "Add Developer" at the top.Fill in the details:Dev ID: This determines the filename (e.g., 0_devendrn).Socials: Add GitHub, YouTube, or Discord links.Copy the generated JSON.Create a new file in the developers/ folder named exactly as your Dev ID (e.g., 0_devendrn.json) and paste the content there.📂 File StructureThis tool is built to support the following data architecture:/ (Root)
├── index.html                   # This Generator Tool
├── shader-list-testing.json     # The main array of all shaders
├── developer-list-testing.json  # Mapping of Dev IDs to filenames
└── developers/                  # Individual developer profiles
    ├── 0_devendrn.json
    └── 1_xenon.json
🤝 ContributingIf you want to improve this tool:Edit index.html directly.It uses Tailwind CSS via CDN, so you can use standard utility classes.Icons are provided by Lucide Icons.Made with ❤️ for the Newb Shaders Community.
